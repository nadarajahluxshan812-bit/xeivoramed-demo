# OPTIONAL. Point the frontend at a deployed XeivoraMed backend to wire the live
# flows (clinic pull, passport, emergency, access log).
#
# Leave this UNSET for a standalone public demo: the app then makes NO network
# calls and shows bundled demo data everywhere (no errors). Set it only if/when
# you deploy the backend, e.g. VITE_API_URL=https://api.xeivora.com
#
# The backend's CORS_ORIGIN must include this site's origin.
# VITE_API_URL=
